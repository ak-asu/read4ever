// Stub — fully implemented in step 4 (Library screen).
// ResourcesDao.watchAll() returns a stream of these; the real implementation
// adds totalCount + doneCount via a GROUP BY join over chapters.
class ResourceWithStatus {
  const ResourceWithStatus();
}
