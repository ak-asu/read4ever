// Stub — fully implemented in step 4 (Continue Reading strip).
// ResourcesDao.watchContinueReading() returns a list of these;
// each pairs a resource row with its lastOpenedChapter.
class ResourceWithChapter {
  const ResourceWithChapter();
}
