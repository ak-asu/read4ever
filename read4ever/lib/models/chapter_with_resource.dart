// Stub — fully implemented in step 9 (Bookmarks screen).
// ChaptersDao.watchBookmarked() returns a stream of these;
// each pairs a chapter row with its parent resource (for display in the list).
class ChapterWithResource {
  const ChapterWithResource();
}
